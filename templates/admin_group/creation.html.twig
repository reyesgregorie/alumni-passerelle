{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center my-5">Créer un Groupe</h1>
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.parent) }}
        {{ form_row(form.tag) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('app_admingroup_index') }}" class="btn btn-outline-secondary">Retour</a>
    {{ form_end(form) }}
{% endblock %}
